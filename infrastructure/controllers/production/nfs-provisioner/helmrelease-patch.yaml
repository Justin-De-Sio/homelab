apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nfs-provisioner-release
  namespace: nfs-provisioner
spec:
  values:
    nfs:
      server: 192.168.1.25
      path: /mnt/MyBigPool/apps/k8s
    storageClass:
      defaultClass: false
