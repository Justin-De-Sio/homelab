apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: calcom-daily-backup
  namespace: calcom
spec:
  schedule: "0 0 3 * * *"
  backupOwnerReference: self
  cluster:
    name: calcom-database
