apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: homarr
  namespace: dashboard
spec:
  interval: 30m
  chart:
    spec:
      chart: homarr
      version: "3.14.0"

      sourceRef:
        kind: HelmRepository
        name: homarr
        namespace: dashboard
      interval: 12h